node_modules/
**/node_modules/
.next/
**/.next/
.env
.env.*

# Committed template. The negation must come after the .env.* rule and
# match at any depth, which a bare filename does.
!.env.example
!**/.env.example
.vercel/
dist/
build/
coverage/

supabase/.temp/